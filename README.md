# hosanna-song

호산나 성가대 칸타타 연습 웹 앱

## 🎵 소개

부활절 칸타타 발표를 위한 파트별(소프라노, 알토, 테너, 베이스) 연습 동영상 재생 웹 앱입니다.

- **발표일**: 2026년 4월 5일
- **도메인**: https://hosanna-song.web.app

## 📋 기능

- 6개 칸타타 곡 + 전체곡 (총 7곡)
- 파트별 비디오 재생 (탭 형식)
- 재생 속도 조절 (0.5x ~ 1.5x)
- 구간 반복 재생 (A-B 리핏)
- 관리자 공지사항 수정
- 반응형 디자인 (모바일/태블릿/데스크톱)

## 🚀 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. Firebase Service Account Key 설정

1. [Firebase Console](https://console.firebase.google.com/) > 프로젝트 설정 > 서비스 계정
2. "새 비공개 키 생성" 클릭
3. 다운로드한 JSON 파일을 `serviceAccountKey.json`으로 프로젝트 루트에 저장

### 3. 데이터 업로드

```bash
# Firestore에 곡 데이터 업로드
npm run upload-data

# Firebase Storage에 미디어 파일 업로드 (시간이 오래 걸릴 수 있음)
npm run upload-media
```

### 4. 로컬 테스트

```bash
# Firebase 에뮬레이터 실행
firebase emulators:start
```

브라우저에서 `http://localhost:5000` 접속

### 5. 배포

```bash
firebase deploy --only hosting
```

## 📁 프로젝트 구조

```
hosanna_song/
├── public/               # 호스팅 파일
│   ├── index.html        # 메인 앱
│   └── res/              # 리소스 (cover.jpg 등)
├── song_data.json        # 곡 데이터
├── upload_data.js        # Firestore 업로드 스크립트
├── upload_media.js       # Storage 업로드 스크립트
├── firebase.json         # Firebase 설정
└── package.json          # npm 설정
```

## 🔑 관리자 기능

- D-Day 영역 클릭 → 비밀번호 입력
- 기본 비밀번호: `admin`
- 공지사항 수정 가능

## 🎯 곡 목록

1. 구원자! 그 이름 예수!
2. 홀로 외로이
3. 십자가에 못 박아라!
4. 그가 채찍에 맞으므로
5. 나 같은 죄인 살리신
6. 부활하신 주, 예수!
7. 칸타타 전체곡

## 📝 라이선스

MIT
